import { DocShell } from '../../../components/DocShell';
import { ApiReference } from '../../../components/ApiReference';
import { Section } from '../../../components/Section';
import { Example } from '../../../components/Example';
import { Lead } from '../../../components/Lead';
import { api } from './api';

import regionBoxes from './_examples/region-boxes.example.txt';
import heatmapCells from './_examples/heatmap-cells.example.txt';
import fixedSizeBoxes from './_examples/fixed-size-boxes.example.txt';
import preBinnedCounts from './_examples/pre-binned-counts.example.txt';
import editableBinHeights from './_examples/editable-bin-heights.example.txt';
import full2DEdgesCornersBody from './_examples/full-2-d-edges-corners-body.example.txt';
import resizeXOnly from './_examples/resize-x-only.example.txt';
import moveOnly from './_examples/move-only.example.txt';

export default function Layout({ children }) {
  return <DocShell>{children}</DocShell>;
}

# Rect

<Lead>The generalized **bar**: each axis independently resolves its extent as a **span** (`x1`/`x2`, `y1`/`y2`), a fixed pixel **size** (`width`/`height`) centred on an anchor, a categorical **band**, or a baseline→value length — so a rectangle can span both axes at once, as in Observable Plot's `rect` (heatmap cells, 2-D regions, binned histograms, annotation boxes). `rect` spans both axes; `rectX` / `rectY` force one axis to a baseline→value length. Prefer `bar` when one axis is a category band and the other a value (classic elicitation bars); use `rect` when both axes are spans/bands, and pair it with `brushRect()` for edge/corner/body editing.</Lead>

<ApiReference entries={api} />

<Section id="basics" title="A 2-D rectangle (both axes are spans)">

Give both axes explicit endpoints and each datum draws a rectangle spanning x1→x2 and y1→y2.

<Example
  code={regionBoxes}
  title="Region boxes"
  blurb="x1/x2 and y1/y2 place each rect; fill by a field."
/>

</Section>

<Section id="heatmap" title="Heatmap (both axes categorical)">

Put a category on **both** axes and each datum draws one cell at that (x, y) intersection — a heatmap. A quantitative `fill` resolves to a sequential colour ramp; a categorical `fill` to a palette. Band scales pad by default, so set `scale: { padding: 0 }` on each axis to close the gap and let the cells touch. Colour isn't gesture-invertible, so edit a cell's value through the ramp: `select()` on the cells makes the one you click the target row, and `legendColor({ edit: edit.legendValue() })` drags that row's value along the ramp.

<Example
  code={heatmapCells}
  title="Coloured cells"
  blurb="A category on x and y; fill by a quantitative field; padding 0 so cells touch."
  try={(
    <>
      <b>Click</b> a cell to select it, then <b>drag</b> the legend handle to set its value.
    </>
  )}
/>

</Section>

<Section id="fixed-size" title="Fixed-size boxes (width/height)">

Give an axis an explicit pixel extent with `width` (x) or `height` (y) and the rect becomes a fixed-size box **centred** on its `x`/`y` anchor — the SVG-native parameterisation, alongside `x1`/`x2` spans and band cells. An explicit size overrides the band interval and the baseline→value length. The anchor is a positional channel, so a `move()` on `x` and `y` moves the whole box.

<Example
  code={fixedSizeBoxes}
  title="Boxes at data points"
  blurb="x/y anchor each box; width/height set a constant 28×28 px size."
  try={(
    <>
      <b>Drag</b> a box to move it (its size stays fixed).
    </>
  )}
/>

</Section>

<Section id="histogram" title="Binned histogram (rectY)">

A histogram is a vertical rect whose `x` is a quantitative bin span (`x1`/`x2`) and whose `y` is a count from the baseline — that is exactly `rectY`. Data must already be binned, one row per bin; there is no binning transform. Prefer `barY` when bins are categorical labels instead of numeric edges.

<Example
  code={preBinnedCounts}
  title="Pre-binned counts"
  blurb="x1/x2 place each bin on a shared quantitative x; y is the count."
/>

<Example
  code={editableBinHeights}
  title="Editable bin heights"
  blurb="Drag on y to reshape the belief distribution; clamp keeps counts ≥ 0."
  try={(
    <>
      <b>Drag</b> a bin’s top edge to change its count.
    </>
  )}
/>

</Section>

<Section id="editing" title="Composable 2-D editing (brushRect)">

`brushRect()` edits all four extents, opt-in and composable: grab an edge to resize one side, a corner to resize two extents, the body to move the whole rect. `resize` (`'both' | 'x' | 'y' | 'none'`) picks which axes resize; `move` (boolean) toggles body-drag — so you can ship resize-one-axis, move-only, or resize-only variants.

<Example
  code={full2DEdgesCornersBody}
  title="Full 2-D: edges, corners, body"
  blurb="Default brushRect(): every direction live."
  try={(
    <>
      <b>Drag</b> an edge to resize a side, a corner to resize two extents, or the body to move.
    </>
  )}
/>

<Example
  code={resizeXOnly}
  title="Resize x only"
  blurb={"brushRect({ resize: \"x\" }) — only the vertical edges resize; body still moves."}
  try={(
    <>
      <b>Drag</b> a left/right edge to resize width; the body to move.
    </>
  )}
/>

<Example
  code={moveOnly}
  title="Move only"
  blurb={"brushRect({ resize: \"none\" }) — no edges grab; the whole rect translates."}
  try={(
    <>
      <b>Drag</b> anywhere in the rect to move it (edges do nothing).
    </>
  )}
/>

</Section>

<Section id="creating" title="Creating rects">

A rect is a **data mark**, so it's creatable like any other (see [Create · Remove · Anchors](/editing/existence)) — with one wrinkle: a click gives a single point, but a rect needs a size, so a creator must seed the extent. A `rectY`-style rect (band or baseline→value length) gets its size from the axis and creates like a `bar`. A 2-D span rect (`x1`/`x2`, `y1`/`y2`) needs the opposite corner supplied through `create({ defaults: { x2, y2 } })`, then `brushRect()` shapes it. Skip the extent and the new row mints zero-size and invisible — the dev build warns you. Geo's `edit.geo.createRect()` is the purpose-built version: it mints a box sized from default spans around the click.

</Section>
