import { DocShell } from '../../../components/DocShell';
import { ApiReference } from '../../../components/ApiReference';
import { Section } from '../../../components/Section';
import { Example } from '../../../components/Example';
import { Lead } from '../../../components/Lead';
import { api } from './api';

import clickToAddAPoint from './_examples/click-to-add-a-point.example.txt';
import existenceMoveOnOneMark from './_examples/existence-move-on-one-mark.example.txt';
import arbitrationClickRecoloursAltClickDeletes from './_examples/arbitration-click-recolours-alt-click-deletes.example.txt';
import clickToAddAnAnchor from './_examples/click-to-add-an-anchor.example.txt';
import seedAWholeLineNewseries from './_examples/seed-a-whole-line-newseries.example.txt';
import freehandDrawAPathAsYouDrag from './_examples/freehand-draw-a-path-as-you-drag.example.txt';
import removeAWholeLine from './_examples/remove-a-whole-line.example.txt';

export default function Layout({ children }) {
  return <DocShell>{children}</DocShell>;
}

# Create · Remove · Anchors

<Lead>Create and remove work like any edit: `create` inverts the clicked pixel into a datum through the same scales `encode` uses, then appends it to the dataset — every mark reading those rows encodes it, composite glyphs included. `edit.line.anchor` / `newSeries` / `draw` extend that to connected paths; `edit.geo.create` / `createRect` / `draw` on [Geo](/marks/geo) invert a map projection instead. `remove` and `edit.line.removeSeries` take rows back.</Lead>

<ApiReference entries={api} />

<Section id="what-can-create" title="What can — and can't — be created">

Creation writes dataset **rows**, so it belongs on a **data mark positioned by an invertible axis** — which is nearly every mark. Only **guides** (they carry no per-row data) and **derived** marks (their position is computed from the whole dataset, not one field) are excluded. If you put a creator where it can't place a datum, the dev build warns you.

<div className="api-table-wrap">
  <table className="api-table">
    <thead>
      <tr><th>Mark</th><th>Create?</th><th>How</th></tr>
    </thead>
    <tbody>
      <tr><td><code>point</code> · <code>dot</code> · <code>symbol</code></td><td>✓</td><td>invert the pointer through x / y directly</td></tr>
      <tr><td><code>tick</code> · <code>bar</code></td><td>✓</td><td>one axis inverts to a band slot, the value axis to the value</td></tr>
      <tr><td><code>face</code></td><td>✓</td><td>positioned like a point — on its own axis or in a scatter</td></tr>
      <tr><td><code>rect</code></td><td>✓</td><td><b>seed an extent</b> (<code>x1</code>/<code>x2</code> or <code>y1</code>/<code>y2</code>) via <code>defaults</code> so it isn't minted zero-size</td></tr>
      <tr><td><code>dotstack</code></td><td>✓ (special)</td><td>one axis is a slot; the stack position is <i>derived</i> from the count</td></tr>
      <tr><td><code>composite</code> (glyph)</td><td>✓</td><td>several marks over one dataset — create on <b>one</b> part; the row renders across all</td></tr>
      <tr><td><code>line</code> · <code>connectedScatter</code></td><td>✓</td><td><code>edit.line.anchor</code> / <code>newSeries</code> / <code>draw</code> — series-aware, needs a series field</td></tr>
      <tr><td><code>geo*</code></td><td>✓</td><td><code>edit.geo.create</code> / <code>createRect</code> / <code>draw</code> — invert the projection (lon/lat)</td></tr>
      <tr><td><code>rule</code> · <code>axis</code> · <code>grid</code></td><td>✗</td><td><b>guides</b>, not data — a guide edits the <i>domain</i> (<code>edit.axis.*</code>), not rows</td></tr>
      <tr><td><code>trend</code></td><td>✗</td><td><b>derived</b>: the line is a fit over every row, so there's no single field to mint</td></tr>
    </tbody>
  </table>
</div>

</Section>

<Section id="gesture-rule" title="Picking the gesture">

`create` defaults to `gesture: "click"`. Give it `gesture: "dblclick"` when a move-drag lives on the same mark, so a press-and-move reshapes the point you're on instead of racing the create — every "create + move" example below uses `dblclick` for this reason. A plain `click` works when nothing else claims the plane.

</Section>

<Section id="create" title="Create — click to add">

`create` is its own edit: a plane click inverts the pointer through the positional channels and appends a datum. Constraints still apply — `count(max)` caps the total.

Declare a creator on exactly one mark. A plane gesture carries no node, so it fans out to every mark's plane-pick edits — put `create` on two marks and one click appends two rows. Direct-pick edits like `move` are immune: they route to the mark you actually touched. The dev build warns if you don't. When multiple edits share one gesture, `when` arbitrates which one claims it.

<Example
  code={clickToAddAPoint}
  title="Click to add a point"
  blurb={"create({ defaults }) mints a datum at the pointer; count({ max }) caps the dataset."}
  try={(
    <>
      <b>Click</b> empty space to add a point · <b>drag</b> to move it.
    </>
  )}
/>

</Section>

<Section id="existence" title="Create, remove & move together">

Mark-level create (dblclick) + remove (Alt-click) + a move drag, all on one mark. When two edits share a gesture, `when` decides which claims the event.

<Example
  code={existenceMoveOnOneMark}
  title="Existence + move on one mark"
  blurb="dblclick adds, Alt-click removes, drag moves."
  try={(
    <>
      <b>Double-click</b> to add · <b><kbd>Alt</kbd>+click</b> a dot to delete · <b>Drag</b> to move.
    </>
  )}
/>

<Example
  code={arbitrationClickRecoloursAltClickDeletes}
  title="Arbitration — click recolours, Alt-click deletes"
  blurb="cycle and remove share the click gesture; when (noAlt vs alt) decides which one claims it."
  try={(
    <>
      <b>Click</b> a dot to recolour · <b><kbd>Alt</kbd>+click</b> to delete · <b>Drag</b> to move.
    </>
  )}
/>

</Section>

<Section id="anchors" title="Anchors — building a connected path">

On a line, `edit.line.anchor()` adds a point to the nearest series, or starts a new one from empty space. `edit.line.newSeries()` seeds a whole line from sampled positions. Order is tracked so the path stays reproducible.

<Example
  code={clickToAddAnAnchor}
  title="Click to add an anchor"
  blurb={"edit.line.anchor({ into: \"nearest\" }) extends the connected path in click order — near or far, it stays one line (into: \"new\" starts a fresh one)."}
  try={(
    <>
      <b>Drag</b> a point, or <b>click</b> empty space to add the next anchor.
    </>
  )}
/>

<Example
  code={seedAWholeLineNewseries}
  title="Seed a whole line (newSeries)"
  blurb="Double-click drops six evenly-spaced anchors at the click value; then sweep to shape."
  try={(
    <>
      <b>Double-click</b> to drop a line, then <b>sweep</b> to shape it.
    </>
  )}
/>

<Example
  code={freehandDrawAPathAsYouDrag}
  title="Freehand — draw a path as you drag"
  blurb={"On an order:\"sequence\" line, draw samples the pointer by distance and appends points in creation order. A later drag over the line reshapes it; a drag in empty space extends the same line rather than starting a new one. Pass into:\"new\" to start fresh lines."}
  try={(
    <>
      <b>Press and drag</b> to draw a path · <b>drag over it</b> to reshape · <b>drag elsewhere</b> to keep extending the same line.
    </>
  )}
/>

<Example
  code={removeAWholeLine}
  title="Remove a whole line"
  blurb="remove (Alt-click) deletes one anchor; edit.line.removeSeries() (click) deletes the whole line — the delete counterpart to anchor / newSeries / draw."
  try={(
    <>
      <b>Alt-click</b> a point to delete just that anchor · <b>click</b> a point to remove its entire line.
    </>
  )}
/>

</Section>
