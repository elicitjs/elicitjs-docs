import { DocShell } from '../../components/DocShell';
import { ApiReference } from '../../components/ApiReference';
import { Section } from '../../components/Section';
import { Example } from '../../components/Example';
import { Lead } from '../../components/Lead';
import { api } from './api';

import aBarMark from './_examples/a-bar-mark.example.txt';
import theSameBarNowEditable from './_examples/the-same-bar-now-editable.example.txt';
import readingTheAnswer from './_examples/reading-the-answer.example.txt';

export default function Layout({ children }) {
  return <DocShell>{children}</DocShell>;
}

# Overview

<Lead>ElicitJS builds charts people answer with — drag a bar, place a point — and you get structured data back. A chart is a declarative `Elicit(spec)`: one **schema**, one **dataset**, and a list of **marks**. Every example on these pages runs the exact code beside it. This page is the wiring; [Core concepts](/concepts) is the mental model.</Lead>

<ApiReference entries={api} />

<Section id="anatomy" title="A minimal chart">

Three pieces make a static chart: `schema` (what each field is and its domain), `data` (the rows), and `marks` (the marks that encode those rows). That is the whole model until you attach an edit.

<Example
  code={aBarMark}
  title="A bar mark"
  blurb="schema + data + bar({ channels }). x is a band of categories, y a linear value, fill a constant."
/>

</Section>

<Section id="encode-edit" title="Encoding, and its inverse">

Attach an edit to a channel and a gesture writes that channel back through the **same** scale. The bar below is identical to the one above, except `y` now carries `edit: move()` — so encoding (data → height) gains its inverse (drag → data).

<Example
  code={theSameBarNowEditable}
  title="The same bar, now editable"
  blurb="y carries edit: move(). Dragging a bar writes its value back to the data."
  try={(
    <>
      <b>Drag</b> a bar up or down.
    </>
  )}
/>

</Section>

<Section id="reading" title="Reading the answer">

Committed data leaves the chart through `onChange`, `getData()`, or `chart.on("change", …)`. Hover previews never fire these — only a finished edit does.

<Example
  code={readingTheAnswer}
  title="onChange / getData"
  blurb={"onChange logs each commit. getData() returns a deep copy of the elicited rows; on(\"change\") is the same seam as a listener."}
  try={(
    <>
      <b>Drag</b> a bar — open the console to see the committed rows.
    </>
  )}
/>

</Section>
